/**
 * B1 stub. Cursor should turn this into a working GLB viewer.
 * Do not import files from vendor except the GLB as a static asset / fetch URL.
 */

const hint = document.getElementById("hint");
if (hint) {
  hint.textContent =
    "Stub running. Load vendor/going-train-core-v1/assets/going-train-core.glb, default +180° around Z.";
}

console.info("Nocturne 40 B1 stub. See CONSTRAINTS.md.");
