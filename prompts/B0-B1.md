You are working in the Nocturne 40 repo I just opened.

Read CONSTRAINTS.md and AGENTS.md first. Follow them exactly.

This session is B0 + B1 only.

Goal:
- Keep vendor/going-train-core-v1/ completely untouched.
- Finish the tiny Vite + TypeScript + Three.js viewer stub in src/ so it loads vendor/going-train-core-v1/assets/going-train-core.glb.
- Parent wrapper group rotates the loaded train +180° around Z by default (product orientation: fourth arbor toward 6 o’clock). Add a simple keyboard or on-screen toggle for 0° vs 180°. Do not bake rotation into vendor nodes.
- Show a simple mm-scale grid or XYZ helper so I can tell orientation.
- Make `npm run check` run `node scripts/check-core.mjs` and pass. Improve that script if needed, but do not require heavy new dependencies unless parsing the GLB needs one small library.
- Create PROGRESS.md (short) when done.

Do not build a case, dial, hands, plates, bridges, strap, or exploded view.
Do not change gear meshes.
Do not add a UI framework.

When you need to run commands, use npm install, npm run check, and npm run dev.

Stop when check prints OK and the viewer loads the train.
